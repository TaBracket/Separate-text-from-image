# itt — OCR (English) — React + Flask

Mono-repo with:

- frontend/ (React, Vite)
- backend/ (Flask API)

Phases: 0) Tooling & repo setup

1. Project skeleton (health endpoint + dev scripts)
2. OCR MVP (detection+recognition)
   ...
